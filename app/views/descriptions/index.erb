<%- @points.each_with_index do |point, index| %>
  <div class="margin-top">
    <%= index + 1 %>.
    <%= link_to point.title, edit_point_path(point) %> -
    <span class="text-muted"><%= point.audio %><span>
  </div>
  <%= point.description %>
<% end %>
